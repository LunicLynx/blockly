goog.provide('Blockly.Masks');

interface IBlocklyStatic {
    Masks: {
        allMasks(workspace: IBlocklyWorkspace):IBlocklyMask[];
    }
}

Blockly.Masks.allMasks = (workspace) => {
    return null;
}